const typeDef = require('./wilayah.typedef');
const resolvers = require('./wilayah.resolver');
const wilayahUtilitiy = require('./wilayah.utilities');

module.exports = {
    typeDef,
    resolvers,
    wilayahUtilitiy
};
